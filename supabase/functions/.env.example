# Server-side only. Never expose these values through VITE_* variables.
DEFAULT_PAYMENT_PROVIDER=stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
APP_URL=http://localhost:8080
# Test-only Mercado Pago PIX. Keep all Mercado Pago secrets outside the Vite app.
# Live marketplace routing is deliberately blocked until NGO OAuth onboarding is implemented.
MERCADO_PAGO_ACCESS_TOKEN=APP_USR-...
MERCADO_PAGO_WEBHOOK_SECRET=...
MERCADO_PAGO_LIVEMODE=false
APP_ORIGIN=http://localhost:8080
